<fieldset>
  <%= form_for(@rank ||= Rank.new, :remote => true ) do |f| %>
      <% if @rank.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@rank.errors.count, "error") %> prohibited this rank from being saved:</h2>

            <ul>
              <% @rank.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
      <% end %>

      <div class="field">
        <%= f.label :name %><br />
        <%= f.text_field :name %>
      </div>
      <div class="field">
        <%= f.label :preferred_name %><br />
        <%= f.text_field :preferred_name %>
      </div>
      <div class="field">
        <%= f.label :display_parent %>
        <%= f.check_box :display_parent %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
  <% end %>
</fieldset>